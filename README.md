# Fashion-MNIST-Neural-Network-to-FPGA
Neural network from scratch in Python/NumPy to classify Fashion‑MNIST images, with planned FPGA deployment via quantized weights and custom Verilog inference engine.
